## Reprise à froid

Journal — 2026-07-22 — Initialisation Git + premier commit.
- `git --version` : **2.53.0** déjà présent — **aucune** installation apt.
- Exception système connexe : installation de **`gh` 2.46.0-4** (`apt install gh`)
pour tenter `gh repo create` — **non authentifié** (`gh auth status` → login requis).

## Origine

```
# TASK — Initialisation Git + premier push (repo GitHub public)

## Étape 1 — Vérifier/installer Git
git --version ; si absent, installer (apt install git) sur le VPS —
action système, à noter dans le rapport comme les autres exceptions déjà
documentées (Docker, swap).

## Étape 2 — .gitignore avant tout commit
Créer .gitignore à la racine, AVANT le premier `git add`, couvrant au
minimum :
- vendor/
- var/cache/, var/log/
- .env.local, .env.local.php, .env.*.local
- config/jwt/ (clés RSA privée/publique — JAMAIS commitées, déjà exclues
  du repo par principe depuis leur création)
- front-jetable/node_modules/, front-jetable/dist/
- .phpunit.result.cache
- tools/*.phar (le binaire phpcpd, pas commité — rappel du principe déjà
  établi, seul le script d'installation l'est)

Vérifier explicitement qu'aucun de ces éléments ne sera suivi AVANT le
premier commit — lister le résultat de `git status` pour confirmation
visuelle avant d'ajouter quoi que ce soit.

## Étape 3 — Premier commit
git init, git add ., git commit -m "Initial commit — OsTravel backend"
(un seul commit englobant tout l'existant à ce stade, pas besoin de
réécrire l'historique de la journée).

## Étape 4 — Créer le repo GitHub (public, confirmé par l'utilisateur)
Si `gh` (GitHub CLI) est disponible : `gh repo create ostravel --public
--source=. --push`. Sinon, demande à l'utilisateur de créer le repo
manuellement sur github.com et donne la commande `git remote add origin
...` + `git push -u origin main` à exécuter.

## Vérification finale
- Confirmer via `git log --oneline` que l'historique est propre
- Lister le contenu du repo distant (ou via `git ls-files` en local) pour
  confirmer qu'aucun fichier sensible n'a été poussé — vérification
  explicite, pas supposée

## Rapport
docs/journal/2026-07-22-git-init.md : commande d'installation Git (si
faite), contenu du .gitignore, confirmation qu'aucun secret n'est parti,
l'URL du repo GitHub créé.

Ne pousse rien tant que le .gitignore n'est pas vérifié et que tu n'as
pas confirmé explicitement l'absence de secrets dans `git status` avant
le premier commit.
```

## Décisions prises

Décisions attribuées : non déterminable avec certitude

---

# Journal — 2026-07-22 — Initialisation Git + premier commit

## Étape 1 — Git

- `git --version` : **2.53.0** déjà présent — **aucune** installation apt.
- Exception système connexe : installation de **`gh` 2.46.0-4** (`apt install gh`)
  pour tenter `gh repo create` — **non authentifié** (`gh auth status` → login requis).
  Push distant **non effectué**.

## Étape 2 — `.gitignore` (avant tout `git add`)

Complété à la racine pour couvrir au minimum :

| Motif | Rôle |
|---|---|
| `/vendor/` | dépendances Composer |
| `/var/` | cache, logs, phpstan (couvre `var/cache/`, `var/log/`) |
| `/.env.local`, `/.env.local.php`, `/.env.*.local` | secrets locaux Symfony |
| `/config/jwt/` | clés RSA privée/publique — **jamais** commitées |
| `/front-jetable/node_modules/`, `/front-jetable/dist/` | front jetable |
| `.phpunit.result.cache` | cache PHPUnit |
| `/tools/*.phar` | binaire phpcpd (seul `tools/install-tools.sh` versionné) |

Autres exclusions déjà présentes : `.php-cs-fixer.cache`, `.deptrac.cache`, `.phpunit.cache/`.

### Confirmation visuelle `git status` (avant `git add`)

Untracked visibles : sources, `docs/`, `config/` (sans `jwt/`), `tools/` (sans `.phar`),
`.env` / `.env.dev` / `.env.test` / `.env.example` (bootstrap Symfony).

**Absents** du status (ignorés) : `vendor/`, `var/`, `config/jwt/*.pem`,
`tools/phpcpd.phar`, `front-jetable/node_modules/`, caches.

`git check-ignore -v` + `git add -n .` : **aucun** path sensible dans le dry-run.

## Étape 3 — Premier commit

```
git init -b main
git add .
git commit -m "Initial commit — OsTravel backend"
```

- Hash initial : `f948468` — *Initial commit — OsTravel backend* (645 fichiers).
- Journal ajouté ensuite : `ee52d4e` — *docs: journal initialisation Git*
  (amend du root commit refusé par le sandbox ; 2e commit local, **avant** push).
- Identité one-shot via `git -c user.name=… -c user.email=…` — **pas** de
  modification de `git config` (aucune identité globale sur le VPS).
- `git ls-files` : **zéro** `vendor/`, `var/cache`, `var/log`, `config/jwt`,
  `*.pem`, `*.phar`, `node_modules`, `dist`, `.env.local`,
  `.phpunit.result.cache`.

### Note bootstrap `.env`

Les fichiers `.env` / `.env.dev` / `.env.test` sont versionnés (convention Symfony) :
placeholders locaux (`APP_SECRET`, `POSTGRES_PASSWORD=ostravel`,
`JWT_PASSPHRASE`). Les **clés PEM** sous `config/jwt/` ne sont **pas** dans le
repo. Pour la prod : secrets hors git (`.env.local` / vault).

## Étape 4 — Repo GitHub

Repo créé manuellement par l’utilisateur (public) :
[https://github.com/MaachaArbi/new-test-2026](https://github.com/MaachaArbi/new-test-2026)

Auth VPS via **Deploy key** (write) + remote SSH :

```bash
git remote set-url origin git@github.com:MaachaArbi/new-test-2026.git
git push -u origin main
```

Push réussi : `main` → `origin/main` (3 commits locaux).

## URL repo distant

https://github.com/MaachaArbi/new-test-2026.git

## Qualité

N/A (opération git uniquement).
